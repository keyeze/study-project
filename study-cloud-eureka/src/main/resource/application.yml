eureka:
  client:
#   是否注册自己
    register-with-eureka: true
#   是否检索服务,(因为是注册中心,不需要依赖其他服务)
    fetch-registry: true
  instance:
    appname: eureka-service
spring:
  application:
    name: eureka-server
